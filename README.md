# codsoft_task1
Random class is used to generate a random number between 1 and 100.

Scanner class is used to take the user’s input.

Loops (while and do-while) are used to control attempts and allow replay.

If–else statements are used to check whether the guess is too high or too low.

Variables are used to store the number, attempts, score, and user choice.
